---
description: Core project conventions and coding standards for Janeh by Manoush
alwaysApply: true
---

# Project Conventions

## Stack

- React 18 + TypeScript (strict mode) + Vite
- Ant Design 6 (antd) for all UI — prefer antd components over custom HTML/CSS
- Axios for API calls via `src/api/http.ts`
- react-router-dom v6 for routing
- i18next + react-i18next for translations (EN, RU, HY)
- Playwright for E2E tests
- pnpm as package manager — pin exact versions (no `^`)

## File Structure

```
src/
  api/          # API client and endpoint modules
  app/          # App entry, providers, layout, routes
  components/   # Shared reusable components
  config/       # Environment config
  consts/       # Constants, enums, route definitions
  i18n/         # i18n config + locales/ (en.json, ru.json, hy.json)
  pages/        # Page components grouped by feature folder
  styles/       # Global CSS (minimal — antd handles most styling)
  types/        # Shared TypeScript interfaces and enums
  utils/        # Pure utility functions
```

## Imports

- **Same folder:** use relative `./file` (e.g. `import { X } from "./types"`).
- **Elsewhere in project:** use path from project root, e.g. `src/components/ProductCard`, `src/api/http`, `src/types/product`.
- Do not use path aliases (e.g. no `@/`).

## Code Style

- Named exports only (no default exports except i18n and component entry points — see Component folder structure).
- `React.FC` with explicit props interfaces for components.
- Arrow functions with explicit return types for utilities.
- `as const` for constant objects, derive types with `typeof`.
- Use `void` prefix for fire-and-forget promises.
- Prettier: double quotes, semicolons, 2-space indent, trailing commas, 90-char width.

## Simplicity (do not overcomplicate)

- Prefer simple, readable code over clever or abstract solutions. Avoid unnecessary abstractions, extra layers, or speculative features.
- Do not write a lot of CSS. Use antd components and layout (Flex, Space, Row/Col) first; add CSS only when needed (e.g. one-off spacing, image sizing, gradients).
- Keep `styles.module.css` minimal: target desktop by default and add a small number of responsive breakpoints (e.g. 768px, 576px) only where layout or readability require it. Good responsiveness is important; avoid dozens of media queries or redundant rules.

## Component folder structure

Every component lives in a folder whose name is the logical component name. Use these files:

| File | Purpose |
|------|--------|
| `index.tsx` | Component implementation; export the component (default export for the folder). |
| `styles.module.css` | Component styles (required if custom CSS is needed). |
| `types.ts` | Optional; props and other types used only by this component. |
| `utils.ts` | Optional; pure helpers used only by this component. |
| `consts.ts` | Optional; constants used only by this component. |

- Do not put type definitions, constants, or utils inside `index.tsx` if they can live in the dedicated files above.
- Do not write any inline styles — use `styles.module.css` or global styles.

## UI Rules (antd-first)

- Use antd `Typography` (Title, Paragraph, Text) instead of raw `h1`, `p` tags
- Use antd `Flex`, `Space`, `Row`/`Col` for layout instead of custom CSS
- Use antd `Result` for error/empty states (e.g., 404 page)
- Use antd `App.useApp()` for `message`/`notification` (not static imports)
- Use antd `Dropdown` with `GlobalOutlined` icon for the language switcher
- Configure antd theme via `ConfigProvider` in `AntdProvider.tsx`
- CSS modules only for what antd cannot handle (gradients, image sizing, responsive containers). Keep CSS minimal; prefer antd for layout and spacing.
- No inline styles — use CSS modules when custom CSS is needed.
- Desktop-first layout; add media queries only where needed for good responsiveness (e.g. stacking columns, smaller padding on small screens).

## Translations (i18n)

- All user-facing text must come from translation files — never hardcode UI strings
- Translation files: `src/i18n/locales/{en,ru,hy}.json`
- Keys are nested by section: `nav.*`, `home.*`, `catalog.*`, `product.*`, `about.*`, `contact.*`, `notFound.*`, `admin.*`, `common.*`
- Use `useTranslation()` hook in components, pass `i18n.language` to locale-aware utils
- When adding new UI text, add keys to all three locale files

## API Layer

- All HTTP calls go through `src/api/http.ts` (axios-based `http<T>` generic)
- Endpoint modules in `src/api/*.api.ts` (e.g., `productsApi`, `ordersApi`)
- Types for API responses live in `src/types/`

## Types

- Shared domain types in `src/types/` (Product, Order, etc.)
- Component-local props and types go in that component’s `types.ts` (or in `src/types/` if shared).
- Enums for domain values (ProductCategory, OrderStatus) in `src/types/`
