import { GALLERY_PAGE_IMAGES } from "src/consts/gallery";

/**
 * `/gallery` page only — order and membership of images on the full gallery view.
 * Base list comes from `src/consts/gallery`; adjust here for page-specific subsets.
 */
export const galleryPageImages = GALLERY_PAGE_IMAGES;
