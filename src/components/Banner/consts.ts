/** Fallback home banner video when GET /v1/banner-content fails or returns no URL */

export const BANNER_VIDEO_URL =
  "https://dfgkytohsu2cm.cloudfront.net/static_new/հոմ փեյջ սկզբնամասը.mov";

/** When false, failed/empty API shows static hero image only (no video). */
export const BANNER_VIDEO_ENABLED = true;
